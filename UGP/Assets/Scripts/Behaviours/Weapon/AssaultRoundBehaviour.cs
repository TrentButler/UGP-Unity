using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace UGP
{
    public class AssaultRoundBehaviour : MonoBehaviour
    {
        public float DamageDealt;

        //public void Fire(Transform barrel, float range)
        //{
        //    GetComponent<Rigidbody>().velocity = barrel.forward * range;
        //    Destroy(gameObject, 4);
        //}

        //NEEDS WORK
        //DESTROY BULLET WHEN IT COLLIDES WITH SOMETHING
        //private void OnCollisionEnter(Collision collision)
        //{
        //    Destroy(gameObject);
        //}
    }
}